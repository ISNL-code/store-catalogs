import { Typography } from '@mui/material';

const StoppedForService = () => {
    return (
        <Typography variant="h2" sx={{ width: '100vw', height: '100vh', textAlign: 'center', mt: 20 }}>
            Servicing...
        </Typography>
    );
};

export default StoppedForService;
